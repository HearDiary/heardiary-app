import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <h1>HearDiary MVP</h1>
    <p>This is a minimal app.</p>
  </React.StrictMode>,
)
